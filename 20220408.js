// 1. 手写bind

// 2. instanceof原理

// 3. 写一个sleep函数（等待几秒后执行）

// 4. 场景题：比如说我有20个请求，然后需要限制执行，并发请求只有4个

// 5. 写一个反转链表（2次）

// 6. 防抖和节流

// 7. 写一个快排

// 8. 写一个反转二叉树

// 9. 手写Promise.all

/**
 * a为何值时下述表达式成立：
 * var a = ?
if (a==1 && a==2 && a==3) {
console.log(1)
}
// a = {i:1, valueOf: () => a.i++}
 */